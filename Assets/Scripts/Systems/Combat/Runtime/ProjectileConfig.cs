using UnityEngine;

public class ProjectileConfig : MonoBehaviour
{
    public ProjectileStats stats;  // assign an asset in the inspector
    public Object owner;           // set by spawner if needed
}
